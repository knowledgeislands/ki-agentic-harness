import { mechanical, one } from './shared.ts'
export const CONFIG_1 = mechanical('CONFIG-1', 'Opt-in marker and keys', 'A keyless `[ki-tools]` marker is present and validated down.', 'WARN', (c) => { if (!c.applicable) return one({ status: 'NOT_APPLICABLE', message: 'not applicable: no [ki-tools] declaration or bin/ marker' }); if (c.config === 'missing') return one({ status: 'VIOLATION', message: 'configuration file is absent — add a [ki-tools] opt-in marker', subject: '.ki-config.toml' }); if (c.config === 'malformed') return one({ status: 'VIOLATION', message: 'configuration file is malformed', subject: '.ki-config.toml' }); if (c.config === 'absent') return one({ status: 'VIOLATION', message: 'no [ki-tools] table — add the opt-in marker', subject: '.ki-config.toml' }); return c.configKeys.length ? one({ status: 'VIOLATION', message: `unknown key under [ki-tools]: ${c.configKeys.join(', ')}`, subject: '.ki-config.toml' }) : one({ status: 'PASS', message: '[ki-tools] table present', subject: '.ki-config.toml' }) }, (c) => c.conformConfig())
export const CONFIG = [CONFIG_1] as const
