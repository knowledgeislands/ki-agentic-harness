/**
 * Eval scenarios for the `ki-plugins` skill — the plugin-marketplace repo shape.
 *
 * Design note: a capable model can reason generically about "a plugin marketplace". These
 * scenarios target house-ARBITRARY specifics a baseline cannot derive: that the marketplace
 * repo is a generated, byte-for-byte projection of the harness (never hand-edited), that the
 * projection is skills+agents only with MCP deferred (no `.mcp.json`), that agents are
 * flattened from `agents/governance/`, and that generation/enablement belong to `ki-binding`
 * while `ki-plugins` owns only the on-disk shape.
 */
import type { Scenario } from '../harness.ts'

export const scenarios: Scenario[] = [
  {
    skill: 'ki-plugins',
    id: 'plugins-generated-not-edited',
    prompt:
      'A skill in the knowledgeislands/ki-plugins marketplace repo has a typo in its SKILL.md. How do you fix it, and why not just edit the file in that repo?',
    assertions: [
      { name: 'edit the harness source', re: /harness|source|skills\//i },
      { name: 'regenerate via build-plugin', re: /build-plugin|regenerat|ki:binding/i },
      { name: 'projection is generated, not source', re: /generated|projection|byte-for-byte|not hand-?edit/i }
    ],
    rubric:
      "House model: ki-plugins is a lossy, byte-for-byte PROJECTION of the ki-agentic-harness (ADR-KI-HARNESS-003) — the harness `skills/`/`agents/governance/` are the single source of truth and the marketplace is generated by `ki-binding`'s `ki:binding:build-plugin`. You fix the typo at the harness source and regenerate; editing the projection directly is wrong because the next regeneration overwrites it. A correct answer names editing the harness source + regenerating, and explains the repo is generated, not hand-maintained."
  },
  {
    skill: 'ki-plugins',
    id: 'plugins-mcp-deferred',
    prompt:
      'Does the knowledge-islands plugin in the ki-plugins marketplace ship the Knowledge Islands MCP servers? What does it ship, and what would auditing it flag about MCP?',
    assertions: [
      { name: 'skills and agents only', re: /skills?\b.*agents?|agents?\b.*skills?/i },
      { name: 'MCP deferred / host-local / no .mcp.json', re: /defer|host-?local|no\s+\.?mcp\.json|sandbox/i },
      { name: 'flags an unexpected .mcp.json', re: /\.mcp\.json|flag|warn/i }
    ],
    rubric:
      "House rule: the plugin ships `skills/` and `agents/` ONLY (v1) — no `.mcp.json`. The KI MCP servers are host-local and do not run in Cowork's gVisor sandbox, so they are deferred, not projected. `audit-plugins.ts` WARNs if a `.mcp.json` appears in the plugin. A correct answer says skills+agents only, MCP deferred (sandbox reason), and that an `.mcp.json` would be flagged."
  },
  {
    skill: 'ki-plugins',
    id: 'plugins-vs-binding-lane',
    prompt:
      'The ki-plugins repo is enabled in Claude Cowork but its projected skills look out of date. Which skill owns the Cowork enablement (marketplace registered, plugin turned on) versus the on-disk projection being correct and current?',
    assertions: [
      { name: 'ki-binding owns enablement', re: /ki-binding|BIND-4|enablement|cowork_settings|enabledPlugins/i },
      { name: 'ki-plugins owns on-disk shape', re: /ki-plugins|projection|on-?disk|marketplace\.json|shape/i },
      { name: 'stale projection → regenerate', re: /stale|regenerat|build-plugin|out of date/i }
    ],
    rubric:
      "Lane split: `ki-binding` (BIND-4) owns cross-surface ENABLEMENT — the machine's `cowork_settings.json` `extraKnownMarketplaces` + `enabledPlugins` pointing at the repo. `ki-plugins` owns the repo's ON-DISK projection shape and correctness. A stale projection is a `ki-plugins`-audited finding fixed by regenerating via `ki-binding`'s build-plugin — never by editing the projection. A correct answer assigns enablement to ki-binding, on-disk shape to ki-plugins, and resolves staleness by regeneration."
  }
]
