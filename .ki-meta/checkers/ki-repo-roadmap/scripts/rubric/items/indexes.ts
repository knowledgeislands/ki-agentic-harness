import { mechanical } from './common.ts'
export const INDEX_1 = mechanical('INDEX-1', 'thematic index projection', '`docs/roadmap/README.md` exactly matches the generated theme index, list-based active-plan sections, and dependency graph.', 'FAIL', true)
export const INDEX = [INDEX_1] as const
