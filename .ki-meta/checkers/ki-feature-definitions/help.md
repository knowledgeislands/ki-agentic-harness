# ki-feature-definitions

Codify, audit, and maintain Feature Definitions — the behaviour-level specification of what a system does — in any Knowledge Islands repo.

**Invoke:** `ki-feature-definitions audit [dir] | conform [dir] | help | educate [dir] | new <area> "<title>" | refresh`

**Modes:**

- `AUDIT`
- `CONFORM`
- `EDUCATE`
- `HELP` — explain this skill and stop; the default when no mode is given (then routes, if interactive)
- `NEW`
- `REFRESH`

**See also:** ki-decision-records (the governing decisions a requirement cites), ki-authoring (Markdown/TOML style).
