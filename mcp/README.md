# mcp

Knowledge Islands **MCP servers**. **Empty for now**.

Today KI's MCP servers live as separate `mcp-*` repos (governed by the **`knowledgeislands-mcp`** skill under [skills/](skills/)). This
directory is where they would consolidate as Bun workspace packages (`mcp/<name>/`) if the harness takes them in — each conforming to the
workspace-MCP standard that `knowledgeislands-mcp` defines.
